# 共创企业助手 V0.2.6 Windows x64 测试包手工冒烟验收

请在自动生命周期脚本成功且当前候选已经重新安装后执行。每项填写“通过”或“失败”，失败时保留截图和界面可复制的错误正文。任何凭据不得写进本文件或截图。

## 测试信息

- Windows 版本与内部版本号：
- 设备架构：
- 测试时间：
- `windows-device-acceptance.json` SHA-256：
- 文件属性显示的发布者：

## 首次启动与设置

- 城市首次选择可用，选择后可以在设置中切换杭州、绍兴、金华、宁波或默认全部：
- 首次新对话默认显示 OpenCode Go、DeepSeek V4 Flash、Max：
- 个人资料可更换头像，重启后保持：
- 设置中的检查更新可点击；正式源未配置时明确提示，不能伪报最新版：
- 点击主窗口关闭按钮后窗口隐藏、托盘图标仍在且后台任务继续；从托盘可重新打开，只有“退出应用”才结束进程：

## Windows 文件与命令通道

- 未安装 WPS 时“打开方式”不显示 WPS；安装 WPS 后才显示且能打开 DOCX、XLSX、PPTX：
- 默认打开使用 Windows 文件关联；“选择打开方式”弹出系统选择器，未安装 Office/WPS 时不伪造应用选项：
- “在文件资源管理器中显示”会定位并选中目标文件，不只打开父目录：
- 使用 PowerShell 执行分别产生随机 stdout、非零退出码和测试目录文件副作用的三条命令；结果必须逐项真实变化，不能是固定日志、固定退出码或无文件副作用：

## 三路模型真实对话

- OpenCode Go 中转站：连接验证成功，真实回复成功，重启后无需重新输入凭据：
- 原生 DeepSeek：连接验证成功，真实回复成功，重启后无需重新输入凭据：
- 自定义 OpenAI 兼容 API：地址、模型和凭据验证成功，真实回复成功，重启后保持：
- 三路来回切换不串用模型或凭据，错误代码与错误正文可复制：
- OpenCode Go、硅基流动和 Orgarid 都能从模型菜单底部手动刷新；刷新中不能重复触发，成功后去重并更新当前会话，失败后清空旧目录并显示不可用：
- 首次启动自动刷新一次模型目录；用户点击“刷新模型”时再刷新一次，切换模型不发额外验证请求：
- 输入框上方用量栏保持单行，仅显示本轮、会话、缓存、花费和今日：

## 报告模式与产物

- 企业全景 A／B／C 仍分别代表报告分析模式，选择和生成流程均可用：
- 项目预评估、可行性报告以及两者同时出具的业务模式均保留：
- 未要求格式时，每一份报告只生成一份可编辑 DOCX，不自动生成 PDF：
- 明确要求 PDF 后，客户端从同一份定稿内容导出 PDF，不发起第二轮模型写作；DOCX 和 PDF 内容一致：
- 生成的 DOCX 能用 Microsoft Word 或 WPS 重开；按需 PDF 能重开、提取正文并完成自动逐页视觉样例：

## 技能中心与 MCP

- 技能市场和已安装技能为两个页面；安装后卡片保留并显示已安装：
- 共创精选、魔搭、腾讯 SkillHub、第三方仓库可切换、翻页、查看详情：
- 从魔搭或 SkillHub 安装一项社区技能，重启后保持且不能覆盖内置技能：
- 企查查和天眼查连接后实际发现工具，并各完成一次最小企业查询：
- PaddleOCR 对一张含文字图片完成识别；坏凭据不会显示已就绪：
- 共创联网检索详情可看到真实工具，但侧栏没有独立联网检索入口：

## 自动化与企业空间

- 新建一项绑定企业空间的自动化任务，可编辑首次时间、周期和完整提示词：
- 点击立即运行后创建正常结果会话；最近运行记录可打开，暂停和恢复有效：
- 创建两个企业空间并分别新建会话，资料、会话和最近对话互不串用：
- 左侧可按企业名或会话标题筛选最近对话，总览页仍可打开：

## 门禁、恢复与可访问性

- 自动生命周期脚本中的运行时签名破坏阶段确实失败关闭，恢复后才可启动：
- 模型生成中强制关闭应用，重启后该轮显示已停止且可以继续同一会话：
- Tab、Shift+Tab、Enter、Space、Esc 可操作主要按钮、弹窗和焦点循环：
- 1024×700 及当前屏幕分辨率下无横向溢出，弹窗内容可滚动：
- 页面、日志、截图和回执中未出现账号密码、API 密钥、Token 或完整 Authorization：

## 生产服务与更新待复核项

- 登录时服务端能观测到真实请求，客户端收到成功回执；登录失效后不显示旧头像：
- 下载页只把 `.exe` 显示为安装包；SHA-256 文本不得成为下载项、打开方式或安装文件：
- 点击检查更新时，只有成功读取 Windows `latest.yml` 且版本相同时才显示“当前已是最新版”；404、无清单和网络错误必须显示失败：
- 有更高版本时点击更新可完成下载、安装并自动重新打开客户端：

## 结论

- 总体结论：通过 / 失败
- 失败项编号与复现步骤：
- 证据目录：
