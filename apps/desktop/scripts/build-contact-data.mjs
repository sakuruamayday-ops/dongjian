import { readFileSync, writeFileSync } from 'node:fs'

const root = new URL('../../../', import.meta.url)
const bytes = readFileSync(new URL('docs/assets/contact/wechat-qr.jpg', root))
const output = new URL('packages/client/ui-gongchuang-product/src/client/contact-data.ts', root)
writeFileSync(output, '/** Bundled public contact QR; generated by build-contact-data.mjs. */\nexport const WECHAT_QR_DATA_URL = ' + JSON.stringify('data:image/jpeg;base64,' + bytes.toString('base64')) + '\n')
