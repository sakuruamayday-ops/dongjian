/** Node-owned SQLite adapter used by the bundled Graph Memory Community core. */

import { DatabaseSync } from 'node:sqlite'

export { DatabaseSync }
/**
 * Type contract for database sync instance.
 */
export type DatabaseSyncInstance = DatabaseSync
