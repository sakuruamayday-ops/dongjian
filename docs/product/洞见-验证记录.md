# 洞见 0.1.0 验证记录

验证日期：2026-09-08。对象为独立工作树的未提交候选代码，不代表已发布制品。

## 已执行

| 范围 | 可复现命令 | 本轮结果 |
| --- | --- | --- |
| 界面及宿主组件 | `pnpm run test:gui` | 331 个测试文件通过，4433 项通过，1 项跳过 |
| 专业校验、免账号、MCP、附件和产品 UI 联动 | `pnpm exec vitest run packages/guard/gongchuang-policy-gate packages/product/gongchuang-account packages/mcp/gongchuang-connectors apps/desktop/tests/document-import.spec.ts apps/desktop/tests/composer-drafts.spec.ts packages/client/ui-gongchuang-product` | 53 个测试文件、644 项通过 |
| 真实文件与内置模板 | `python3 product/dongjian/tests/test_neutral_runtime.py` | 7 项通过，覆盖真实 DOCX、XLSX、HTML、PDF、24 份内置模板及原生 DOCX 生成器 |
| 桌面构建 | `pnpm --filter @gongchuang/desktop run build` | 完整构建已通过；最终安装制品仍待验收 |

两组 TypeScript 测试存在范围重叠，不相加作为独立测试总数。文件测试使用带 PyMuPDF、python-docx、openpyxl、Pillow 和 lxml 的 Python 运行环境。

文件测试确认：去除已知旧水印后保留正文、用户图片和表格公式；PDF 不追加水印；错误页数仍被拒绝；内置模板封面、页眉、媒体及 XML 无旧品牌文字；原生 DOCX 生成器不写入默认出版者元数据。模板处理脚本仅定位产品自有模板副本，不处理用户资料。

## 仍待完成

- GitHub 源码快照范围、文档检查收尾、提交及制品回读。
- 实际外部模型与知识服务调用、下载隔离属性下的 Gatekeeper 安装体验、Windows 与 Intel Mac 实机验证。

当前未上传 GitHub，未发布安装包，未改动网站、网站下载入口或原产品更新渠道。组件测试不等同于真实安装验收，模型服务的实际调用仍需单独验证。

## 最终 Mac 候选实机检查

知识库向导无效登录链接已移除，连接界面 10 项测试通过。最终源码重新构建，Mac arm64 包经打包后签名、运行时与技能验签通过，以全新隔离用户数据目录真实启动。首次设置显示“洞见企业空间”；自定义临时资料目录可保存；模型与连接设置可打开，模型密钥为空；知识库初始未启用，向导仅提供地址和凭据输入，无产品登录链接。技能列表实际显示已安装 51 项。未向任何模型或知识服务提交测试凭据。

| 制品 | 字节数 | SHA-256 |
| --- | --- | --- |
| Dongjian-0.1.0-mac-arm64.dmg | 285824602 | 31e6a3e943d52a763d79d2b9c006122a64f84ee38fbc8ce783daab6054f6b5bd |
| Dongjian-0.1.0-mac-arm64.zip | 291112089 | 7bc7e657359c14946deb302fe9e52dcfc28754a55ad6562246e43a6289cf1ea5 |

`pnpm run verify-client-ui-i18n` 通过，检查 494 个界面源文件。文档检查为 13 项通过、2 项未通过：独立中文产品文档及签名技能源 README 的双语范围问题，以及主线历史验收记录引用未随源码携带的 3 个本地文件。README 导航锚点和语言切换缺失已修复，文档检查尚待复验，不记为全绿。待上传源码路径已排除 `.build`，对 Git 跟踪及非忽略文件扫描未命中常见私钥和令牌模式；该模式扫描不等于完整秘密检测。

## 候选补充验证

桌面单元与安装输入契约测试 50 个文件、329 项通过。Python 技能、文档提取和无默认水印回归 56 项及 24 个模板子测试通过。原版 Python 运行目录因测试产生的 500 个未签名字节码缓存已可恢复移入系统废纸篓；原有签名文件逐项哈希未发生变化。后续 Python 测试禁用字节码缓存。

独立策略、运行时与技能发布公钥指纹已配置；私钥位于仓库外。审批材料保存在本地构建目录 `.build/dongjian-approval-20260908-01/`，包括已验签上游快照、84 项技能文件差异、目标文件哈希、影响报告和测试记录。用户确认后，哈希复核及治理验证通过，状态为 `approved`，独立策略、技能和 macOS arm64 运行时已签名。

最终候选综合回归 106 个文件、991 项通过。打包输入验签及实际文档依赖运行验证通过。首轮 macOS arm64 DMG/ZIP 已生成，应用以干净隔离用户目录实际启动，显示洞见、本机用户及未配置模型提示，无产品登录墙。首次启动发现企业根目录旧名称残留，已修复为“洞见企业空间”，路径、IPC 和企业侧栏联动 3 个文件、66 项测试通过；修复后的最终包仍需重建并复验。

macOS 当前为 ad-hoc 签名，不是 Apple Developer ID 公证发行，不能承诺下载后无 Gatekeeper 提示。GitHub 目标已确认是 `sakuruamayday-ops/dongjian` 私有仓库，尚未上传。实际外部模型调用及其他平台安装尚未验证。
