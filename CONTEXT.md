# 共创企业助手交付语境

本术语表统一文档生成、视觉验收、平台验收和上游底座升级所使用的产品语言。

## 当前发布事实

V0.4.1 已于 2026-09-02 从精确源 `eac8855411044f6445a82b04a3e5aad1c25689e7` 正式发布 macOS arm64、macOS x64 和 Windows x64，服务器登记 `release_id=18`、制品 ID 52 至 54，是当前唯一生产正式版。客户端当前版本由单一共享源派生，服务器下载、更新和修复路由读取实际 `published` 记录。V0.4.0 已退役并作为唯一上一版保留；V0.3.3 制品已进入服务器可恢复回收区，数据库历史与回滚证据继续存在。

V0.4.1 采用 DeepSeek Harness `dsh-v0.1.2-alpha.4` 的不可变提交 `4e84901e6471b79ec0338099867ebb4606d12bb5`。除 alpha.3 的连接恢复、Remote 错误载体、会话投影、全会话轮次导航、图片消息、Windows 原子写重试和单轮统计外，本版采用官方相邻 Agent 双向消息、Profile 请求头模型发现、模型目录搜索、长会话渲染优化、共享 Web Fetch 和强类型会话读取；共创继续负责企业自动化、模型中转站、账号与系统凭据、企业空间和附件、签名技能运行时、专业策略、美元花费估算及桌面更新。Harness 升级采用官方优先原则：官方实现完整覆盖当前产品消费方时即采用并移除共创重复代码，官方缺失或尚不能满足真实消费方时才保留产品实现，并在下一版本继续审计。

V0.4.0 同时修正 macOS 免费公测首装签名链：afterPack 只校验 macOS 包内容，Electron Builder 在 Electron fuses 后用 `mac.identity: "-"` 完成最终 ad-hoc 签名；Builder 不重签已由运行时索引绑定的原生载荷，afterSign 在 DMG/ZIP 生成前只读复验最终 App 的代码签名、运行时和技能。源 `08ebc1a0cc` 故障候选已经作废。主人指定在本机现有用户环境验收，Safari 从生产域普通直链下载最终 DMG 后，首次打开被 Gatekeeper 拦截且未显示“已损坏”，“隐私与安全性”真实出现“仍要打开”，二次确认后应用正常启动并保留既有登录态。公开下载页由提交 `e52a82f56222a27d8bdf88bab6300a0963562f27` 经 CI `33471398688` 部署为 `20260901T045541Z-e52a82f56222-40251670`，已在 Safari 登录页确认三步图形帮助上线且不再包含终端或 `xattr`；该回执不冒充全新用户或 VM。

V0.3.3 保留管理门户当前设备版本修复，并正式交付签名清单身份绑定的 macOS 断点续传、下载进度与剩余时间、退出后续传和受控残留清理；设置中的归档页只保留已归档企业空间与会话，并支持恢复或直接删除；账号卡只展示身份；每个本地自动任务固定复用一个结果会话，并以本次派发消息的精确 `messageId` 和轮次归属结果，避免繁忙会话中的普通消息被误记为任务执行。门户源 `e07b53f2a50adbd15e5f06e1ee307e43c3922603` 已部署为 `20260827T033718Z-e07b53f2a50a-2b4504a4`。

实际打包的 V0.4.0 arm64 与 x64 应用均已更新至精确 V0.4.1，完成签名清单发现、归档与目标版本验签、应用交换、GUI 启动、提交、凭据密文和用户状态保留；故障路径均恢复旧应用。最终 arm64 包还用已保存加密凭据完成 OpenCode Go DeepSeek V4 Flash Vision Exp 首次回复、单次退出、重启保持和第二次回复。当前 macOS 使用 ad-hoc 代码签名，更新器保证不主动删除 userData 与凭据文件，但不能承诺系统 Keychain 跨版本绝不要求重新授权。

V0.4.1 内嵌 formal V1.6.15、51 项技能。发布事务已把 `skill_bundle_version=1.6.15` 与三端客户端记录一并写入。客户端版本与内嵌技能版本独立演进。

三平台正式制品从同一精确源生成。macOS arm64 当前机与 x64 Rosetta 已完成生命周期、ad-hoc 深度签名、formal 运行时与 V1.6.15 技能验签和实际打包更新；最终 arm64 ZIP 还通过真实模型窗口收发与重启保持。Windows x64 完成 ZIP、ASAR、PE 架构、原生载荷、运行时和技能静态验收，按已确认边界未签名直接发布，不冒充 V0.4.1 Windows 原生真机通过。

服务器正式事务已把 V0.4.1 发布为 `release_id=18`，V0.4.0 退役为唯一上一版，并将 V0.3.3 制品移入服务器可恢复回收区。服务器事务后从公网完整下载两份 Mac ZIP 与 Windows EXE，精确大小和 SHA-256、Ed25519 清单、三端 Range、三平台修复路由、`latest.yml`、`/readyz`、数据库 current/previous 及 GitHub 无安装资产均通过；见 [`public-update-source-acceptance.json`](/Users/zsh/JiaotangData/release-candidates/gongchuang-client-v0.4.1/eac8855411-desktop-release/public-update-source-acceptance.json)。

机器可读 `productionBaseline` 已切换为客户端 0.4.1、formal 技能 1.6.15、alpha.4 `4e84901e6471b79ec0338099867ebb4606d12bb5`、发布源 `eac8855411044f6445a82b04a3e5aad1c25689e7`、release_id=18。V0.4.0 的源对象、制品摘要和服务器回滚记录继续保留，不再作为当前生产基线。

formal V1.6.15 内嵌于 V0.4.1。它在既有 Office/WPS 内容读取与深度澄清收敛基础上，加入有限专业交付、企业数据源有限降级，以及每次含模板发布的 29 份 Office 与 4 份源码模板全量检查。上述变化不改变默认 Word、按需 PDF 或企业全景 A／B／C 业务模式。

双架构为 ad-hoc 签名，没有 Developer ID 和 Apple 公证。Windows NSIS 安装器和主程序仍未签名，没有 Authenticode 或 RFC 3161 时间戳；Windows 本版只形成静态验收。V0.4.1 继续保留运行时 Ed25519、精确文件集合和逐文件 SHA-256 验签；OpenCode Go 真实模型账号采用本版最终包当日回执，其他外部连接器和专业 PDF 内容证据采用已归档真实回执并披露时点。主人明确免除 V0.4.1 重复 Gatekeeper UI 验收，继续采用 V0.4.0 已验证的设置放行逻辑。Mac 临时下载、演练目录和可重建构建缓存在证据归档后移入 macOS 废纸篓，只保留正式制品与验收回执。

## 规范用语

**回答深度**：
独立于专业领域的本轮输出范围，取值为单点问答、限定分析或正式交付。企业名、项目名、专业术语和输入长度只帮助选择领域，不自动提高回答深度。
_避免使用_：专业技能等级、报告复杂度

**单点问答**：
只回答用户当前提出的问题，并提供结论和最小必要依据；不得自动补齐完整企业画像、项目评分、材料清单或年度规划。
_避免使用_：简版报告、报告前置

**限定分析**：
只围绕用户明确指定的若干维度展开；未被请求的其他专业维度保持不展开。
_避免使用_：完整分析、正式报告

**正式交付**：
用户明确要求报告、申请书、正式材料或选定企业全景 A／B／C 模式后启用的完整专业链路，继续执行结构、证据、计算、来源和文件验收。
_避免使用_：专业问题、长问题

**正式产物**：
客户端生成、关键事实、证据、结构和计算均满足对应 Skill 交付规则后，才能表示为正式完成的文档。普通样式提示和非关键建议不改变正式产物身份。
_避免使用_：输出文件、生成文件

**待完善稿**：
客户端已经生成但仍存在关键交付缺口的可检查文档。它供用户审阅和补充资料使用，不表示可以直接提交；客户端状态、文件名和用户主动导出的草稿均须与正式产物明确区分。
_避免使用_：正式成品、校验失败文件、不可用文件

**关键交付缺口**：
会使文档不能取得正式产物身份的事实、证据、结构或计算问题。样式差异、非关键建议和校验器自身异常不属于关键交付缺口；校验器异常属于产品故障。
_避免使用_：所有警告、任意格式差异、技术报错

**机器交付合同**：
一个正式交付画像中章节、表格、证据槽位、关键要求和产物身份的唯一机器可读来源。专业 Skill 说明方法和判断边界，模板负责呈现，两者均通过稳定身份引用机器交付合同，不得各自维护相互冲突的结构副本。
_避免使用_：提示词要求、模板文字即规则、模型自行判断

**交付画像关键要求**：
由具体正式交付画像声明、未满足时使文档只能成为待完善稿的要求。不同报告、申请书、专利和表格可以声明不同关键要求，共享执行引擎不得以一张通用标题清单替代领域差异。
_避免使用_：全局统一门禁、任意警告、模型临时决定

**待完善稿导出**：
用户取得待完善稿副本用于审阅和补充资料的操作。导出不改变产物身份，也不能把尚有关键交付缺口的文档表示为正式产物。
_避免使用_：强制转正式版、绕过校验、禁止查看草稿

**合同版本归属**：
一份正式产物或待完善稿绑定其生成时实际使用的机器交付合同版本。后续合同更新不追溯改写或重新定性历史文件；用户开始新修订时才采用当时有效的新合同。
_避免使用_：历史文件自动升级、新规则追溯生效、静默重写

**模型无关交付标准**：
所有受支持模型提供方共享同一机器交付合同、关键要求和产物身份规则。模型选择只改变生成能力和性能，不改变专业交付标准。
_避免使用_：模型专属报告规则、切换模型绕过要求

**本地交付诊断**：
为待完善稿和产品故障保存在当前设备上的结构化问题记录。主界面只呈现问题和可执行动作，技术细节按需展开；未经用户主动同意，不向外提交诊断或客户资料。
_避免使用_：自动上传日志、直接展示堆栈、未知错误

**Skill 候选激活**：
一个新 Skill 版本通过机器合同、模板及关联运行时验收后，替换当前正式 Skill 版本成为客户端活动版本的操作。候选验收失败时保持当前正式版本，不在用户任务中临时猜测或降级合同。
_避免使用_：先安装后验证、运行时改用提示词、错误候选在线试错

**模板随 Skill 验收**：
新 Skill 候选包含、修改或引用交付模板时，模板属于该候选的必验收依赖。模板未完成对应结构、生成、打开和呈现检查时，Skill 候选不能激活或表示为正式发布。
_避免使用_：只检查 Skill 文本、模板发布后再测、模板与 Skill 分离验收

**模板验收矩阵**：
模板随 Skill 验收采用的可复现检查集合。每次发布对全部登记模板执行快速静态检查，对本次新增、修改、引用的模板及共享模板的消费方执行深度生成、重开和呈现检查；macOS 与 Windows 均覆盖生成和重开，测试资料使用覆盖长文本、空值、合并单元格、分页、百分比和日期边界的脱敏合成数据。
_避免使用_：只测修改文件、真实客户资料测试、单平台双击检查

**正式模板源**：
随 Skill 提供并用于生成正式产物的可稳定解析模板文件。现有正式模板源统一规范为 DOCX、XLSX、PPTX 等结构可验证的现代格式；DOC、XLS、WPS 等旧式格式不得继续作为正式模板源。该限制不适用于用户输入文件。
_避免使用_：旧式二进制模板、用户附件格式白名单、模板与输入共用限制

**模板基线验收**：
新模板规则首次启用前，对全部现有正式模板执行的一次完整结构、生成、重开、跨平台和视觉验收。新增或发生视觉影响的模板由人工查看全部页面并形成参考基线，后续发布再按模板验收矩阵执行增量检查。
_避免使用_：历史模板自动通过、每次全量人工复验、只测新模板

**Skill 与客户端演进边界**：
机器合同或正式模板源只使用当前活动运行时已经支持的能力时，Skill 可以独立发布；渲染器、任务编排状态或客户端交互发生变化时，必须随支持该能力的客户端候选一并验收和发布。
_避免使用_：每次 Skill 都发客户端、Skill 永远随客户端发布、运行时不兼容热更新

**开放文件接收**：
用户可以把任意文件加入当前任务，客户端不以扩展名白名单阻止接收。系统自动识别并处理正式支持的格式；未知、损坏、加密或当前无法解析的内容必须给出明确状态和可执行处理办法，不得表示为已经读取。
_避免使用_：所有格式保证解析、扩展名上传白名单、读取失败后静默继续

**确定性正式检查**：
依据机器交付合同对结构、证据和计算执行的可重复检查。只有这类检查中的关键交付缺口可以阻止文档取得正式产物身份；模型评审只提供非阻塞建议，不得决定产物身份。
_避免使用_：模型主观门禁、随机评分阻止交付、取消全部校验

**未登记交付类型**：
用户要求但当前活动 Skill 中尚无机器交付合同或正式模板源的文档类型。系统可以生成待完善稿；对应合同和模板完成登记与验收前，该文档不能取得正式产物身份。
_避免使用_：自由生成正式成品、完全拒绝起草、临时猜测合同

**正式身份取得**：
文档的全部交付画像关键要求通过确定性正式检查后，自动取得正式产物身份。该身份只表示符合生成时绑定的 Skill 合同，不代替用户决定是否提交、发送或对外使用。
_避免使用_：自动替用户提交、重复确认弹窗、模型宣布完成

**阶段进展**：
一个任务阶段新取得可复用事实、证据、转换结果、文件修订或明确状态变化。单纯重复同一调用、同一错误、同一校验或没有改变文件哈希的复验不属于进展。
_避免使用_：工具返回即有进展、模型继续思考、重复校验

**无进展暂停**：
外部调用达到自身超时，或当前阶段持续没有形成阶段进展时，停止继续消耗资源并保留现场的可恢复状态。正常持续产生进展的复杂任务不因统一总时长被强制截断。
_避免使用_：全任务固定超时、后台无限等待、失败后继续尝试

**用户中止保留**：
用户主动停止当前执行时，终止后续工具和模型调用，但保留已确认事实、证据、文件及阶段断点；用户后续明确继续时从该现场建立新修订。
_避免使用_：停止即删除、无法取消、后台继续运行

**重启后任务暂停**：
客户端重新启动时恢复尚未完成的用户任务及其上次阶段，但不自动继续访问数据源或消耗模型额度。用户明确继续后才恢复执行。
_避免使用_：启动即续跑、重启丢失任务、后台静默恢复

**用户可见任务进度**：
客户端向用户展示当前阶段、已完成阶段、是否等待用户，以及能够可靠测量的操作进度。工具调用、回执和技术日志默认折叠，不以旋转图标或内部调用数量代替真实进展。
_避免使用_：仅显示运行中、展示全部内部日志、虚构剩余时间

**顺序数据源降级**：
一个专业字段按当前配置的来源顺序取得事实，首选来源成功后不重复调用其他聚合来源；首选不可用时自动切换下一来源。网络、超时和服务端临时错误最多重试一次，权限、额度、参数和不支持错误不重试。
_避免使用_：同时调用全部来源、永久错误重试、每次要求用户选来源

**授权取消降级**：
用户关闭或返回第三方来源授权页面时，本轮将该来源视为不可用并继续顺序数据源降级。客户端保留重新授权入口，但任务不等待、不失败，也不在后台重复打开授权。
_避免使用_：授权弹窗卡死、关闭后再次弹出、取消导致整项任务失败

**字段来源裁决**：
具体 Skill 为专业字段声明来源优先级和冲突处理方式。官方登记、政策及司法来源优先于聚合平台，企业内部经营和财务事实使用用户提供的可靠资料；关键事实仍无法确认时形成待完善稿，非关键字段标记未核验后继续。
_避免使用_：模型猜测冲突、任一来源失败即停止、聚合平台覆盖官方来源

**交付运行时兼容声明**：
正式 Skill 版本声明其机器交付合同、模板和任务状态所需的最低客户端运行时能力。旧客户端继续使用最后一个兼容的正式 Skill 版本，不激活依赖其不支持能力的新候选。
_避免使用_：所有客户端强装最新 Skill、版本不兼容在线试错、强制旧客户端停用

**旧任务合同连续性**：
客户端或 Skill 更新时，尚未完成的任务继续绑定开始该修订时使用的合同版本并恢复为暂停。用户开始新修订时才采用当前兼容的正式合同，不在处理中途静默换规则。
_避免使用_：任务中途升级合同、更新后自动续跑、删除未完成任务

**有限终态验收**：
对全部正式交付画像注入错误合同、缺资料、数据源失败、授权取消、校验器故障、重复错误、客户端重启和用户中止等可达故障，证明任务在有限步骤内进入正式产物、待完善稿、等待用户或明确失败状态。macOS、Windows 打包运行时和模板验收矩阵均属于验收范围。
_避免使用_：单元测试即完成、发布后等用户发现、运行中不得结束

**确定性修复**：
不补造事实、不改变专业判断，能够由既定合同和模板唯一确定结果的单次修订，例如固定标题、表头、日期格式或模板结构修正。
_避免使用_：模型自由重写、补齐缺失证据、修改直到通过

**用户驱动修订**：
待完善稿形成后，由用户补充资料、确认选择或明确要求继续而开始的新修订。等待期间不继续后台执行，新修订复用已确认事实、证据和文件进度。
_避免使用_：后台自动重试、重新开始整个任务、无限纠正

**PDF 源文件**：
用于生成一份最终 PDF 的不可变工作区产物。
_避免使用_：PDF 草稿、渲染输入

**PDF 导出**：
从一个已绑定 PDF 源文件生成且不替换既有产物的最终 PDF 产物。
_避免使用_：打印结果、转换文件

**逐页栅格化**：
用于视觉验收的一份最终 PDF 的有序、完整页面图像集合。
_避免使用_：截图、生成预览

**视觉验收**：
人工对一组逐页栅格化结果中的全部页面作出明确接受或拒绝。成功导出、结构性重开或生成图像均不属于视觉验收。
_避免使用_：视觉检查、渲染成功

**自动视觉回归**：
把最终 PDF 的完整逐页栅格化结果与已确定来源的参考图像作自动比较，用于发现渲染变化；它不包含人工决定，也不得表示为用户已验收。
_避免使用_：人工视觉验收、自动验收通过

**视觉回归参考**：
与最终 PDF 逐页栅格结果比较的来源已记录页面图像。固定黄金样例用于判定跨版本和跨平台变化，同源打印布局预览只用于判定导出链是否一致。
_避免使用_：内容正确性证明、用户验收基准

**视觉差异处置**：
自动视觉回归超出已记录容差时阻止发布候选，并生成逐页差异图供定位；零像素差异不是默认判定标准。
_避免使用_：仅警告、人工验收

**视觉模型路由**：
当前所选提供方中明确声明并经产品验收为支持图片输入的模型路径。模型名称含有 Vision 不构成能力证据。
_避免使用_：Vision 开关、图片模型

**提供方模型目录**：
客户端从已配置模型提供方直接取得的当前模型身份集合。它只说明端点当前列出的模型，不证明图片、推理或文件传输能力。
_避免使用_：客户端固定模型表、连接模型数量

**最近可用模型目录**：
客户端在本机保留的某一提供方最近一次成功模型目录，只能用于恢复审计，不能在刷新失败时作为当前可选目录或表示提供方仍可用。
_避免使用_：团队模型目录、在线最新目录

**模型能力投影**：
由已安装适配器的精确模型目录向发送路径提供的图片输入、推理强度或文件传输能力。模型选择列表不展示这些技术标签，也不得按名称猜测或自动继承能力。
_避免使用_：模型名称推断、选择器能力徽标

**失效模型选择**：
当前选定模型不再出现在一次成功刷新的提供方模型目录中。目录刷新失败不等于模型已经失效，模型改名也不得被推断为原模型的等价替代。
_避免使用_：自动改名、连接失败模型

**目录暂不可用**：
提供方模型目录因超时、断网或临时服务错误无法刷新，但当前模型尚未被成功目录或鉴权结果证明失效。它不同于凭据失效和模型已下线。
_避免使用_：空模型目录、模型已删除

**手填模型候选**：
为不提供模型目录的兼容端点手工指定的精确模型身份。配置阶段验证凭据与路由；选择阶段不追加对话探针，未由适配器声明的图片、推理或文件传输能力不开放。
_避免使用_：待验证模型、自定义全能力模型

**替代模型确认**：
当前模型失效后，用户明确选择后续消息使用的模型。确认只改变当前会话的后续模型和未来新会话默认值，不重写其他会话的历史模型身份。
_避免使用_：自动迁移模型、批量改写会话

**模型目录刷新**：
Host 在客户端启动时执行一次目录刷新；此后只有用户点击“刷新模型”才再次刷新。打开选择器、切换模型、切换推理强度和网络恢复均不刷新，也不发送模型验证请求。
_避免使用_：打开即刷新、切换即验证、后台模型探测

**模型列表**：
按提供方分组，只显示模型名、选中标记和刷新命令；适配器声明推理强度时另行显示推理强度选项。列表不展示已验证、待验证、最近同步或文本、图片、推理、文件标签。
_避免使用_：模型验证状态、能力徽标、同步时间

**草稿文件导入**：
用户通过选择器或拖放导入常规文件后，Host 将副本写入本次对话的受控导入目录，客户端以会话级附件状态保存相对路径并等待用户自行发送。发送时相对路径只进入模型文本；有文字时聊天气泡、排队项、插话和重载历史只显示用户原始输入，仅附件发送时显示简洁文件标签。
_避免使用_：上传即发送、附件自动开始任务

**草稿文件引用移除**：
用户点击输入框文件卡片的关闭控件后，客户端只从当前待发送消息中移除该文件的受控相对路径引用。它不删除、移动或改写源文件、导入副本、企业空间记录及其他对话引用。
_避免使用_：删除文件、移入废纸篓、修改企业空间、清理其他对话引用

**精确同参工具熔断**：
同一 agent 在一个用户轮次内连续调用完全相同的工具和规范参数时，第二次执行后收到恢复提醒，第三次及后续同参尝试在工具执行前拒绝。更换工具、更改参数或开始新的用户轮次即可恢复；该机制不对近似调用作模糊判断。
_避免使用_：通用轮次上限、第三次弹窗审批、模糊循环判定

**Windows 关闭偏好**：
Windows 用户对主窗口右上角关闭动作的本机选择，取值为缩小到托盘或直接有序退出；不改变 macOS 关闭到菜单栏的行为。
_避免使用_：全平台强制托盘、强杀退出

**已稳定账号恢复**：
启动投影登录身份前，等待串行化 Host 刷新完成 Keychain 或 Credential Manager 凭据引用恢复，避免以旧的未登录快照覆盖恢复结果。
_避免使用_：自动登录缓存、明文密码恢复

**生产版本基线**：
当前唯一生产版的客户端、技能、底座、发布源和服务器发布记录组合；机器可读值来自 `product-manifest.json` 的 `productionBaseline`。
_避免使用_：开发工作树版本、下一候选发布版

**单版本发布候选**：
把本轮确认的底座、渲染、模型、更新、交互和收敛变更作为一个不可拆分的下一版本候选。任一必需能力或目标平台发布条件未通过时，整个候选仍未达到正式发布状态。
_避免使用_：分批正式发布、部分完成版本

**提供方文件传输**：
某一模型提供方为上传、限期保存和复用图片请求字节而提供的内部传输机制。远端文件身份只属于对应端点和凭据作用域，不能跨提供方复用。
_避免使用_：团队知识库、通用文件库、Files 功能

**可引用正文**：
当前会话中同一条已定稿用户或助手消息里的普通文字、可见链接文字或代码文字。工具控件、输入框和跨消息选区不属于可引用正文。
_避免使用_：对话界面文字、整段对话

**选文续问动作条**：
可引用正文形成非空文字选区后出现的轻量操作入口；选区由双击、指针拖选或键盘选择形成时具有同一语义。
_避免使用_：双击菜单、复制菜单

**报告模式**：
决定报告分析范围和业务结构的选项，例如企业全景 A／B／C，以及项目预评估报告、可行性报告或两者同时出具；它不表示 Word／PDF 文件格式。
_避免使用_：双版本格式、Word／PDF 模式

**报告默认产物**：
每一份已选报告默认只生成一个可编辑 DOCX；只有用户明确要求 PDF 时，才从同一份定稿内容导出 PDF，不再进行第二轮模型写作。
_避免使用_：默认双格式、企业全景模式取消

**专业续步收敛**：
共创专业策略对一次正式交付修订的可达性和进展进行判定；能力不可用时形成待完善稿并结束当前执行，只有确定性修复可以自动执行一次，仍未通过时等待用户驱动修订。
_避免使用_：通用 Agent Loop 上限、失败后持续修改、无限纠正

**真机验收**：
在对应目标平台的物理设备上运行一个打包候选并形成的验收记录。旧版本通过不等于已验收发生变更的新候选。V0.3.3 的结构化物理设备证据包含当前 arm64 Mac；macOS x64 以同一源提交候选的 x64 原生构建、验签、真实生成重开、自动视觉样例和当前 arm64 Mac 上的 Rosetta 验收形成发布证据，不要求 Intel Mac 物理设备。V0.3.3 Windows 只形成跨构建静态验收，主人确认 V0.2.0 三端实际测试通过是独立历史事实，不能扩写为 V0.3.3 Windows 原生真机通过。
_避免使用_：平台支持、构建成功

**底座目标**：
为某一共创版本选择的、作为适配基线的不可变 DeepSeek Harness 上游发行版。
_避免使用_：最新上游、master

**上游发布审计**：
正式打包前实时查询产品采用清单中的 GitHub 发行版或分支。源码底座和必需方法适配移动时停止打包并要求审阅；没有随包分发的设计参考只报告变化，不自动更新或阻止无关发布。
_避免使用_：自动跟随主分支、所有参考项目必须锁死

**自动检查更新**：
客户端启动后及其后每六小时执行一次非阻塞版本发现，不下载、不安装，也不重启客户端。
_避免使用_：自动升级、静默更新

**一键自更新**：
用户明确确认后，由客户端完成下载、校验、安装与重启的更新流程。V0.2.6 及后续 Mac 版本按签名目标版本执行；V0.2.3 至 V0.2.5 因已发布二进制缺陷需先手工覆盖一次。
_避免使用_：后台自动安装、强制升级

**macOS 跨版覆盖安装**：
已安装 V0.1.4 的用户按历史路线手工覆盖 V0.2.0；已安装 V0.2.3 至 V0.2.5 的用户先退出旧版，再从受控门户下载与芯片一致的 V0.2.6 DMG，将新版拖入“应用程序”并在系统询问时选择“替换”。受控门户不提供或要求过渡程序；此前目标源码更新演练只作机制和恢复证据，不作为旧版二进制跨版本成功证据。
_避免使用_：网站必须先运行过渡程序、兼容桥、旧版一键升级

**V0.2 分发权威**：
受控门户承担首次安装、Mac DMG 和 Windows NSIS 下载，V0.2 分平台更新源承担后续版本发现与下载。当前生产版 V0.4.1 不发布 GitHub 客户端安装资产，GitHub Latest 不是客户端发布、批准或回滚权威。
_避免使用_：GitHub 发布前置、双更新权威

**模型连接唯一入口**：
用户配置模型提供方、凭据和模型身份的唯一产品界面。底座内部授权接口不构成第二个用户可见登录中心。
_避免使用_：通用登录中心、双凭据入口

**客户端版本兼容状态**：
账号服务根据客户端提交的规范语义版本返回支持、建议升级或必须升级；客户端本地只验证版本格式，不维护服务端兼容版本清单。
_避免使用_：客户端硬编码版本白名单、任意版本字符串

**更新提交**：
新客户端完成约定的启动验收后，更新事务才保留新安装；未提交的更新按平台恢复策略处理。
_避免使用_：下载完成、安装成功
